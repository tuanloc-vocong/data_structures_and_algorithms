// Title: Check numeric characters
// Source: Big-O
// Difficulty:
// Time Complexity:
// Topic:

/**
 * Viết chương trình đếm xem chuỗi đã cho có bao nhiêu ký tự là số.
 *
 * Input:
 * Một dòng duy nhất là chuỗi ban đầu, bao gồm các ký tự từ ‘A’ → ‘Z’, ‘a’ → ‘z’, ’0’ → ‘9’ và khoảng trắng,
 * độ dài của chuỗi không quá 1000 ký tự.
 *
 * Output:
 * Một dòng duy nhất là số lượng ký tự là số trong chuỗi.
 *
 * Example 1:
 * Input:
 * Thu 6 la ngay 15 thang 9
 *
 * Output:
 * 4
 **/

#include <iostream>

using namespace std;

int main()
{
    cout << "Hello, World!!!";
    return 0;
}