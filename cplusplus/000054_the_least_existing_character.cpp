// Title: The least existing character
// Source: Big-O
// Difficulty:
// Time Complexity:
// Topic:

/**
 * Cho N chuỗi, hãy viết chương trình tìm ký tự xuất hiện ít nhất trong các chuỗi, chuỗi ký tự chỉ bao gồm
 * các kí tự từ ‘A’ → ‘Z’ và ‘a’ → ‘z’.
 *
 * Lưu ý: không phân biệt chữ hoa hay chữ thường.
 *
 * Input:
 * Dòng đầu tiên là số lượng chuỗi N (1≤N≤1000).
 *
 * N dòng tiếp theo, mỗi dòng là một chuỗi ký tự có độ dài không quá 1000 ký tự.
 *
 * Output:
 * N dòng, mỗi dòng là một ký tự in hoa của ký tự xuất hiện ít nhất trong chuỗi, nếu nhiều ký tự xuất hiện
 * ít nhất thì xuất ra ký tự có thứ tự từ điển nhỏ nhất.
 *
 * Lưu ý: Thứ tự từ điển theo chiều tăng dần là từ ‘A’ đến ‘Z’.
 *
 * Example 1:
 * Input:
 * 3
 * cd
 * Abcdd
 * Bigocoding
 *
 * Output:
 * C
 * A
 * B
 **/

#include <iostream>

using namespace std;

int main()
{
    cout << "Hello, World!!!";
    return 0;
}